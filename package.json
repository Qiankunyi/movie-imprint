{
  "name": "movie-imprint",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:ai-live": "node scripts/ai-validation.mjs --provider=gemini",
    "test:e2e": "node scripts/visual-check.mjs --test-only",
    "screenshots": "node scripts/visual-check.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "playwright": "^1.55.0"
  }
}

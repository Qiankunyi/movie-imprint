# 电影印记（Movie Imprint）

当前仓库已经包含第一条可操作的本地纵向切片：

```text
首页记录流 → 底部记录层 → #作品名／列表输入 → IndexedDB 自动保存与恢复
→ 记录卡 → 成品详情 → 个人态度与独立推荐判断 → 查看／编辑／新增记忆卡片
```

## 本地运行

要求 Node.js 20 或更高版本。不需要数据库、密钥或网络服务。

```bash
pnpm install
pnpm dev
```

然后打开 `http://127.0.0.1:4173`。

需要在同一 Wi-Fi 的手机上临时验收时，可在启动前设置 `HOST=0.0.0.0`，再使用电脑的局域网地址访问。局域网 HTTP 只用于输入、保存与切后台测试；完整 PWA 安装仍应使用 HTTPS 或手机上的 `localhost` 调试连接。测试结束后恢复默认的 `127.0.0.1`，避免把 AI 接口持续暴露给局域网。

首次打开会写入两条人工合成的演示记录。草稿与记录只保存在当前浏览器的 IndexedDB；地址后加 `?clean=1` 可在不写入演示记录的新浏览器环境中检查空状态。

记录层的列表按钮可以插入 `1. ` 或 `- `。列表中回车会自动续写下一项，在空项目上再次回车即可退出；这些前缀与换行作为原始纯文本保存。

`#系列/作品` 可以作为输入时的系列速记，但 `/` 不属于作品正式标题。本地成品只显示末段作品名；确认 Bangumi 条目后，会自动显示条目的规范标题与链接，原始标签仍原样保留。

### 可选启用 Bangumi 作品候选

本地记录不依赖 Bangumi。项目默认使用用户授权的 `qiankunyi/movie-imprint/0.1` 作为 Bangumi 开发者／应用 User-Agent；可在启动服务前通过环境变量覆盖：

```powershell
$env:BANGUMI_USER_AGENT="qiankunyi/movie-imprint/0.1"
pnpm dev
```

显示名称“钱坤一”保留在项目说明中；HTTP 请求头使用 ASCII ID，避免中文头部编码兼容问题。可以另行设置 `BANGUMI_ACCESS_TOKEN`；搜索接口不把令牌发送到浏览器。上游失败时，已经保存的原文与本地作品不会受影响。

完成一条记录后，应用会在后台搜索最多十个候选；有歧义时在首页标记“待确认作品”，由用户在详情页选择正确条目或保留本地作品。确认只更新稳定作品身份，不改写原始感想。

### 可选启用 TMDB（第二数据源）

TMDB 与 Bangumi **共存**，不是替代关系：Bangumi 负责日本动画、动画电影、TV Anime / OVA / ONA，TMDB 负责真人电影、欧美电影与广义电影搜索。用户不需要选择数据源——片单里只有一个「搜索作品」，结果按「本地已有 → 外部数据库」分组呈现。

两个环境变量二选一即可：

```powershell
$env:TMDB_ACCESS_TOKEN="<v4 read access token>"   # 推荐，走 Authorization 头
# 或
$env:TMDB_API_KEY="<v3 api key>"                  # 走查询参数
$env:TMDB_LANGUAGE="zh-CN"                        # 可选，默认 zh-CN
```

**都不配置也不会报错**：TMDB 搜索返回空候选，搜索面板照常展示 Bangumi 与本地结果。密钥只在服务端使用，不下发到浏览器；海报同样通过同源代理获取，不向浏览器暴露外部图片地址。

作品类型不会因为 TMDB 把条目标成 movie 就判定为真人电影——日本动画电影在 TMDB 上同样是 movie。只有类型数据明确时才自动判定，否则保持「未分类」，由用户在作品页认领。

首页会从已经确认 Bangumi ID 的作品中按本地日期稳定选择今日壁纸。图片通过同源代理获取并缓存，不向浏览器暴露外部图片地址；离线时优先恢复本地缓存，无图或失败时使用内置中性壁纸。封面来源会在首页标明，图片不会进入记录导出或 GitHub 数据。

首页右上角的偏好设置可以换一张、固定或关闭作品壁纸，也可以关闭“自动整理新记录”。关闭后点击“完成”只建立仅原文记录，之后可在详情中随时开始整理。所有偏好只保存在当前设备。

### 可选启用 AI 整理

AI 整理不绑定单一供应商。服务端已提供 Gemini、ChatGPT／OpenAI、Claude、DeepSeek 与 Kimi 的统一适配层；设置中只允许选择已经配置密钥的服务。当前本机使用 Gemini，密钥不会进入浏览器、本地记录或导出文件：

```powershell
$env:GEMINI_API_KEY="你的 Gemini API 密钥"
pnpm dev
```

其他供应商分别使用 `OPENAI_API_KEY`、`ANTHROPIC_API_KEY`、`DEEPSEEK_API_KEY`、`MOONSHOT_API_KEY`。模型名可用对应的 `*_MODEL` 环境变量覆盖。AI 失败、超时或返回无效证据时，原文仍保存在本机，并可稍后重试。

AI 生成的记忆卡片是待确认建议：可以逐张保留、编辑或删除。推荐值永远由用户先选择；之后如点击“从原文整理条件”，AI 只能建议对象、理由和注意事项，用户再次点击后才加入记录。

## 验证

```bash
pnpm test
pnpm test:ai-live
pnpm test:e2e
pnpm screenshots
```

- `test`：标签解析、本地确定性分析与推荐默认空值；
- `test:ai-live`：使用 7 条人工合成短句运行真实 Gemini 质量门；会产生 API 调用，但输出报告不含输入原文、证据原句或密钥；
- `test:e2e`：真实文本焦点、IndexedDB 刷新恢复、完成记录、态度／推荐、记忆卡片持久化；
- `screenshots`：生成 412×915、360×800 的浅色／深色关键状态截图。

完整实现状态、冲突处理、截图索引与后续计划见 [首轮实现交付](docs/IMPLEMENTATION_STATUS_C1.md)与 [C2 作品匹配进度](docs/IMPLEMENTATION_STATUS_C2.md)。

## 产品背景（原始说明）

你正在协助我设计和开发一个名为“电影印记（Movie Imprint）”的独立项目。

【项目定位】

电影印记源自 ACG Life OS 的理念，但作为独立项目进行设计与开发。

它不是影评写作工具，也不是评分网站，而是一个“电影感想记录工具”：帮助我将观看电影后随手写下的真实感想，通过 AI 分析、结构化整理和人工确认，转化为可以长期保存、搜索、统计和追踪变化的个人电影记忆资产。

第一阶段仅支持电影，包括：
1. 动画电影（主要使用场景）
2. 真人电影（次要使用场景）

暂不扩展到 TV 动画、电视剧、游戏、漫画和小说。未来只有在电影版流程得到验证后，才参考其方法分别设计其他媒介的工具。

【核心使用流程】

1. 用户输入电影名和未经整理的原始感想。
2. 系统完整保存原文，不进行覆盖或润色。
3. AI 根据原文生成结构化分析草稿。
4. 用户确认、修改、删除或补充 AI 的分析结果。
5. 系统保存原始文本与经过确认的结构化内容。
6. 导出 Markdown、JSON、TXT 等格式。
7. 支持一键同步到个人 GitHub 仓库。
8. 同一部电影可以保存多次观看和补充记录，用于追踪观点变化。

【结构化记录模块】

一、个人态度（单选）
- 超喜欢
- 喜欢
- 无感
- 不喜欢
- 不好说

单选项必须保持精简。更具体的原因应放在标签、说明或记忆卡片中，不要继续增加总体态度选项。

二、情绪反应（多选）
由 AI 从预设标签库中建议，用户可以增删和修改。标签需要分组显示，避免一次展示过多选项。

三、记忆锚点
使用“可增删的记忆卡片”记录。AI 根据原文自动生成卡片，用户也可以手动追加。

添加卡片时，必须先展示系统化、可视化的预设类型库，例如：
- 故事与表达：场景、台词、剧情转折、结局、主题、观点、设定
- 人物：角色、人物关系、表演、配音
- 视听：作画、镜头、演出、美术、色彩、音乐、音效
- 个人反应：被击中的瞬间、可爱或有趣的点、不适、遗憾、疑问
- 关联记忆：系列联系、其他作品联想、个人经历联想
- 影院体验：影厅效果、观众反应、舞台挨拶、特典、现场事件
- 自定义类型

不同类型可以提供不同的填写提示，但任何问题都不应强制填写。

四、后续关系
以用户手动选择为主，AI 可以提出建议，但不应擅自确认。例如：
- 想重看
- 想查资料
- 想继续相关系列
- 想写长文
- 想与别人讨论
- 想收藏实体版
- 只想保留这次记忆
- 暂无后续计划
- 不想继续接触
- 自定义行动

五、是否推荐
- 会：可以广泛推荐
- 看对象：只推荐给特定人群
- 不会

根据选择继续填写推荐对象、推荐理由、注意事项或不推荐原因。

【电影专属信息】

记录电影的观看场景，但不要在初始页面展示复杂表格。

首先区分：
- 电影院
- 家中或个人设备
- 其他

选择电影院后，再按条件展开：
- 影院名称和城市
- 普通厅、IMAX、Dolby Cinema、ScreenX、4DX 等放映制式
- IMAX GT、激光等制式细节
- 首映、先行上映、重映、应援上映等场次属性
- 声优舞台挨拶、Staff 舞台挨拶、直播转播、映后谈等活动属性
- 入场特典、场刊和限定周边

场次属性和活动属性允许多选。例如一场电影可以同时是“重映”和“声优舞台挨拶”。

选择家中观看后，可以记录：
- 电视、电脑、平板、手机或投影
- 流媒体、电视播出、光盘或本地文件
- 独自观看或与他人观看
- 首次观看或重看
- 是否连续看完

除电影名和原始感想外，其余字段原则上均为选填。

【AI 分析规则】

1. 永远完整保留原始文本。
2. 不把原始感想擅自改写成影评。
3. 只根据原文进行分析，不捏造用户没有表达的体验。
4. 原文没有提供的信息应保持为空。
5. 区分“原文明示”和“AI 推测”。
6. 每项自动判断尽可能提供对应的原文依据。
7. AI 输出只属于待确认草稿，不能直接成为最终记录。
8. 用户修改后的结果永远优先于 AI 判断。
9. 不用作品的网络评价代替用户的个人体验。
10. 不强迫用户填满所有字段。

【产品设计原则】

1. 尽可能减少用户的思考和操作负担。
2. 系统结构可以完整，但每一步只展示当下必要的信息。
3. 使用条件式展开、默认折叠和 AI 预填写。
4. 允许用户直接接受全部合理建议并快速保存。
5. 框架追求完整，但每一条感想不必全面。
6. 原始记录、AI 草稿和人工确认结果必须明确区分。
7. 保存个人资产与公开发布必须是两个独立操作。
8. 优先完成可以实际使用的 MVP，避免过早扩张范围。

【第一阶段 MVP】

- 仅支持动画电影和真人电影
- 输入电影名与原始感想
- AI 生成结构化草稿
- 人工确认五个记录模块
- 系统化记忆卡片类型库
- 电影专属观看场景
- 支持同一电影的多次记录
- 导出 Markdown、JSON、TXT
- 同步到个人 GitHub 仓库

暂不开发：
- TV 动画、游戏、漫画和小说记录
- 微博、豆瓣、Bangumi、Twitter 的自动采集
- 多用户账号和公共社区
- 多博客平台自动发布
- 评分、排行榜和推荐算法
- 复杂的数据可视化

【协作方式】

进行产品设计时，应主动发现字段冲突、流程负担和未来扩展问题，但不要擅自扩大 MVP。

进行开发时，应先说明当前要实现的目标和关键选择，再提供可以运行和验证的成果。重要的数据结构、页面流程和技术选择需要留下文档。

如果我的新想法会显著扩大项目范围，请指出它属于：
- 当前 MVP 必需
- 可以预留但暂不实现
- 未来版本功能

项目的最终目标是：让我可以轻松记录真实感想，同时逐渐积累属于自己的、可长期保存的电影文化记忆。

# 侧边栏图片池

把侧边栏成品图直接复制到这个目录：

`public/assets/sidebar-stills/`

把 4 张图的文件主名依次设为 `sidebar-01`、`sidebar-02`、`sidebar-03`、`sidebar-04`。
扩展名可以是 AVIF、WebP、PNG、JPG 或 JPEG，四张图不必使用同一种格式，也不需要修改代码或登记清单。

建议使用 4:3 横图，至少 1200×900；WebP/AVIF 文件通常更小。

界面会在每天第一次使用时从清单中随机选择一张，并在本地固定到当天结束。
